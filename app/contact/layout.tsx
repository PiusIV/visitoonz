export const metadata = {
  title: "Contact — Visitoonz Art Enterprise",
  description: "Get in touch for custom orders, commissions and enquiries.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
